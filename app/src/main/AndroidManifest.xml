<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.javipercas.endprojectifp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignIn" />
        <activity
            android:name=".HelpLost"
            android:label="@string/title_activity_help"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EditProfile" />
        <activity android:name=".ShowProfile" />
        <activity android:name=".ChangePassword" />
        <activity
            android:name=".HelpFound"
            android:label="@string/title_activity_help_found"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".InteresPoints"
            android:label="@string/title_activity_interes_points"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>